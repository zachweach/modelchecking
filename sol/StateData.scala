package sol

/**
 * Class representing the data in a certain state.
 * @param labels - a List[String] that contains all the labels in the current state
 */
class StateData(val labels: List[String]) {

  /**
   * checks if this StateData object has a given label
   * @param lb - the label we're looking for
   * @return a boolean that is true if the state contains lb, and false otherwise
   */
  def hasLabel(lb: String) : Boolean = {
    // TODO : Implement!
    false
  }
}